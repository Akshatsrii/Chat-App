{
  "name": "nexus-chat",
  "version": "1.0.0",
  "private": true,
  "description": "NexusChat — Real-Time Chat App (Next.js + Strapi + Socket.io)",
  "scripts": {
    "dev:backend": "cd backend && npm run develop",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "setup": "npm run install:all && echo '\n✅ Dependencies installed! Copy .env files and run npm run dev:backend then npm run dev:frontend'"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
